import math

base = int(input("Enter base: "))
pwr = float(input("Enter power: "))
result = math.pow(base, pwr)
#result2 = base ** pwr
#print(f"{base} to the {pwr} power is {result} or {result2}")

print(f"{base} to the {pwr} power is {result}")
