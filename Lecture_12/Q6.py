# Which of the following statements would calculate the average of the values stored in the list values?

values = [81.23,]

print(sum(values) / len(values))
# print(average(values))
# print(values / len(values))
# print(max(values) / min(values))

